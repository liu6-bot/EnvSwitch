# 更新日志

本项目的所有显著变更都记录在这里。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [2.2.3] - 2026-09-19

### 修复

- **切换完之后，列表里还是会少掉之前的版本**（用户原话："切换完之后还是会有少之前的选项"）：
  2.2.2 的「全部纳入 + 只改顺序」只保证**今后**不再删条目，救不回**已经丢掉的**版本。
  真正的根因是：**扫描器原本唯一的"记忆"就是 PATH** —— 某版本一旦从 PATH 里掉出去
  （历史版本切换时删过、用户手改了 PATH），`where` 反查就永远找不到它，
  列表里那一项就永久消失。本机实测：`D:\soft\Java\jdk-17`、`D:\soft\Java\jdk1.8.0_202`
  都还在磁盘上，却因为不在 PATH、又不在 Java 的扫描根里，一个都列不出来（3 个变 2 个）。
  现在加了三层保险：
  - **`known_installs` 安装记忆**：见过一次就记进配置，之后每轮扫描都用磁盘现状复核 ——
    目录/主程序还在就继续列出（来源标「历史记录」），真被卸载了才丢弃（不会阴魂不散）
  - **`learned_roots` 学会扫描根**：见过某版本就把它的**父目录**记成扫描根，浅扫一层
    找回同目录下的兄弟版本（`jdk-17` 旁边的 `jdk1.8.0_202`、`~/.nvm/.../v18` 旁边的 `v20`）
  - **`_seed_from_applied` 历史补种**：从旧的切换记录（`removed_path` / `path_entries` /
    `other_entries`）里反推出安装目录，把"曾经管过、后被删出 PATH"的版本捡回记忆，
    这样连**修复前就已经丢失**的版本也能自动回来，不需要用户手动再添加
- **不再因为"从没进过 PATH"而漏掉版本**：学到的扫描根让"只装了解压版、没配过环境变量"
  的版本也能被发现
- **学到的扫描根只对 `dir` 模式启用**（Java / Go / Maven）：`file` 模式（Python / Node）
  的父目录里什么都有，本机实测把 `D:\soft` 学成 Node 的扫描根之后，
  Adobe Photoshop 自带的 `node.exe` 也被当成了"一个 Node 安装"列出来。
  `file` 模式的"版本离开 PATH"由 `known_installs` 记忆兜住，内置扫描根
  （`C:\Python*`、`~\AppData\Local\Programs\Python\*` 等）也覆盖了同盘多版本的常见布局
- **列表里的错误条目现在可以手动去掉**：来源列显示「历史记录」的条目，
  右键「移除」会把它加进"已忘记"名单，不再靠记忆复活（磁盘文件不动；
  若扫描器仍能实际找到它，会照常显示 —— 藏起来只会更糊涂）

### 新增

- **`tools/test_install_memory.py`**：安装记忆的回归测试（离开 PATH 仍在列表、卸载后正确丢弃、
  学根找回兄弟版本、历史补种、切换不丢版本、不产生重复条目、忘记记错的条目）
- **`tools/diag_switch.py`**：诊断脚本 —— 用假后端 + 临时配置副本完整跑一遍真机切换流程，
  不改真实注册表/配置，用来复现"切换后版本变少"这类问题
- **配置文件新增 `known_installs` / `learned_roots` / `scan_seeded` / `forgotten_installs` 字段**
  （老配置自动兼容，首次运行会自动从旧的切换记录里补种）

## [2.2.2] - 2026-09-19

### 修复

- **装了微软商店版 Python 却"看不见"**（用户原话："python 还是只有一个"）：
  2.2.1 把 `...\Microsoft\WindowsApps\python.exe` 一刀切地当成"假安装"忽略，
  可商店版**真装着**的时候，别名指向的是一个跑得起来的 Python（本机实测 3.12.10）。
  现在分两种情况：别名指向真实存在的程序 → 当成正常安装列出来（来源「商店安装」、
  厂商「Microsoft Store」）；别名只是个空壳（应用已卸载，跑它只会弹应用商店）→ 仍然忽略
- **商店版的切换语义**：切到商店版只把 WindowsApps 目录前置到用户 PATH，
  **不写主目录变量** —— 那个目录塞满各种别名、不是真主目录，写进 `PYTHON_HOME` 等于埋雷；
  还原时也不会反手去 `unset` 一个本来没写过的变量
- **列表里两行同时标「当前」**：`JAVA_HOME` 说 26.0.1、PATH 里实际命中 17.0.12 时，
  两行都会被打上"当前"，用户只会更糊涂。现在「● 当前」只给命令行**真正命中**的那一个，
  变量指向的改用「○ 变量指向」弱标记 —— "变量说 A、实际跑的是 B"这种冲突一眼可见
- **体检误报「命令行里的 python 指向微软商店别名」**：旧判定只要 PATH 里存在这个别名目录就报，
  可实际跑的是 `C:\Python314`。现在只在**商店版真的把命令抢了过去**（按 PATH 顺序实算）
  或别名已经失效时才提示，文案也不再声称"不能切换"
- **「未纳入管理」的修复按钮可能点下去必然失败**：判断商店别名死活时不能拿别名**目录**做判据
  （目录永远不是文件），要顺着目录找到里面的主程序再判

### 新增

- **`tools/test_store_install.py`**：商店安装识别的回归测试（真装着 / 空壳两种现场、
  切换计划不写主目录变量、空壳在动注册表之前就被拦下）
- **`Install.is_default`**：把"变量指向"和"实际生效"两件事分开记录（老的切换记录不含这个字段，读回来默认 False）

## [2.2.1] - 2026-09-19

### 修复

- **「清理系统 PATH 冲突」会把工具从机器上删没**（严重）：旧实现按关键词盲删，
  Python 的关键词 `python` 会把 `C:\Python314`（机器上**唯一**的 Python 入口）
  一起删掉，`nodejs` / `dotnet` / `java` 同理。表现是清理完之后
  「Python 一个安装都扫不到」。
  现在删任何一条系统 PATH 之前都会先模拟一遍：**所有工具的入口是否仍然可达**
  （真实存在的可执行文件才算，商店别名不算），会删没的一律保留并说明原因
- **微软商店的「应用执行别名」被当成真安装**：`...\Microsoft\WindowsApps\python.exe`
  只是个跳板，旧版会把它识别成一个 Python 版本（还带着错误的版本号），
  切过去就把 `PYTHON_HOME` 写成了这串别名路径；命令行里跑 python 反而弹商店。
  现在扫描器直接忽略这类目录，体检里单独用「商店别名（非真安装）」提示
- **「未纳入管理」的修复必然失败**：`file` 模式（Python / Node / .NET）下把**目录**
  塞给了只认主程序的 `add_path()`，用户看到的就是
  「部分修复失败：不是有效的 Node.js 可执行文件」。现在自动换成该目录下的主程序，
  实在找不到就明确标成"需要手动添加"，不再给出会失败的修复按钮
- **扫描结果依赖"从哪儿启动"**：`where` / `which` 用的是进程继承来的 PATH，
  从 VS Code、Git Bash 或某个工具链的终端里启动时，那份 PATH 往往是残缺的，
  于是"少扫到几个安装"甚至"一个都没有"。现在统一用注册表里真正生效的
  「系统 PATH + 用户 PATH」去查
- **Python 装在 `C:\Python314` 这类目录扫不到**：内置扫描根目录补上 `C:\Python*`
  与 `C:\Program Files\Python*`
- **「当前版本」会显示被写坏的主目录变量**：变量指向商店别名这类无效位置时，
  不再拿它当"当前版本"
- **切换旧版本后，系统 PATH 里的旧版本清不掉 / 清不干净**：「不拆安装」闸原本一刀切
  「组里只要有一条不在待删名单就整组保留」，于是 `C:\Python314`（被抢路的根目录）被
  它的 `Scripts` 绑住、删不掉，切换一直不生效。现在改为：当安装的主命令入口
  （`python.exe` 所在的根目录）正在被抢路时，把整组（根 + `Scripts` 等）一起请出去，
  只要 `would_break` 验证每条命令仍有别的来源；不安全则整组保留并说明原因

### 新增

- **体检项「系统 PATH 被改过」**：检测到本工具清理过系统 PATH（有备份）时，
  列出被移除的条目，一键还原（需管理员）。备份之后新加的条目会保留，不会弄丢
- **体检项「主目录变量失效」**：`JAVA_HOME` / `PYTHON_HOME` 指向不存在、
  或不含该工具主程序的目录时明确报出来，一键清除（下次切换会写入正确的值）
- **体检项「商店别名（非真安装）」**：说清命令行里的 `python` 为什么会跑到商店去，
  以及怎么关掉应用执行别名
- 手动「+ 添加路径」选到目录时，自动找该目录（含 `bin` / `Scripts`）里的主程序，
  不再只报一句"不是有效的可执行文件"
- 清理条目时若判断"删掉就会让某个工具找不到命令"，修复结果会明确说明保留了哪些、为什么
- **切换改为「全部纳入 + 只改顺序」模型**：切换某工具时，把它**所有已安装版本**的
  PATH 条目都写进变量（存在才写），再把**选中版本调到最前**、其它版本排其后，
  **绝不删除**任何原有条目。从此「切换之后原来的版本消失」不再发生；来回切哪个版本
  哪个生效，旧版本只是被排到后面。还原时也只摘掉本次新加入的条目，不动用户原有的

## [2.2.0] - 2026-09-18

### 修复

- **主界面侧边栏空白**：启动时漏渲染工具导航，导致只有上次选中的工具可见、
  无法切换其它工具（如 Python）。现在启动即完整显示侧边栏
- **「失效条目」修复无效**：`fix_issue` 中分支名写错（`dead_path` vs `drop_dead`），
  点击清理实际不会删除任何条目，现已修正
- **体检误报**：
  - Chocolatey / Scoop 等"shim 目录"不再被报为「未纳入管理」——那是包管理器的跳板目录，
    不代表真实安装
  - 当 PATH 里存在 `%JAVA_HOME%\bin` 这类引用且主目录变量确实指向该安装时，
    不再误报「未加入环境变量」

### 新增

- **体检忽略功能**：对不想处理的问题（如某些历史遗留条目）可点「忽略选中」永久隐藏，
  随时勾选「显示已忽略」恢复；CLI 对应 `audit --ignore <序号>` / `--ignored` / `--unignore all`
- **体检详情面板**：在对话框底部直接展示选中问题的解释、完整涉及路径与修复方式

## [2.1.0] - 2026-09-18

### 修复

- **消除控制台窗口闪烁**：所有版本探测子进程在 Windows 上改为静默创建
  （`CREATE_NO_WINDOW` + `SW_HIDE`），扫描/切换时不再有一串黑框一闪而过
- **解决"切换了但不生效"**：Windows 系统 PATH 优先级高于用户 PATH 这一根因被显式处理
  —— 检测到冲突后支持一键申请管理员权限清理，动手前自动备份系统 PATH，可随时还原
- **降低误报**：形如 `%JAVA_HOME%\bin` 的"引用变量"条目不再被判为冲突（它会跟着切换一起变）
- 侧边栏"版本数量"不再每次刷新都重扫全部工具，减少无谓的子进程与卡顿

### 新增

- **环境体检（audit）**：检测 6 类问题并支持逐条/批量一键修复
  - 未加入环境变量（装了但 PATH 里没有）
  - 未纳入管理（PATH 里有该工具但 EnvSwitch 没管到）
  - 系统 PATH 冲突（会让切换失效）
  - 被更靠前的条目挡住
  - 失效 PATH 条目（指向已删除目录）
  - 重复 PATH 条目
- 主界面顶部提示条：检测到问题时直接提示并可点开体检
- 切换后若检测到冲突，主动询问是否立即修复
- CLI：`audit` / `systemfix [工具id|all]` / `systempath-restore`
- 新增 `envswitch/core/audit.py` 与 `envswitch/core/elevate.py`（UAC 提权与系统 PATH 修复）

## [2.0.0] - 2026-09-18

首个开源版本。

### 新增

- 三平台（Windows / macOS / Linux）图形界面，左侧工具导航 + 右侧安装列表
- 内置 7 类工具：Java (JDK)、Python、Node.js、Go、Maven、Gradle、.NET SDK
- **自定义工具**：图形化编辑器（目录模式 / 文件模式），支持 JSON 导入分享
- 一键切换全局默认：写入 `JAVA_HOME` 等主目录变量 + 前置 `PATH`
- 「打开已生效终端」：切换后立刻拉起带新环境的终端窗口
- 项目级版本文件：`.java-version` / `.python-version` / `.nvmrc` 等
- 完整可撤销：所有写入均记录在案，一键还原
- Windows 系统级 PATH 冲突检测与提示
- CLI：`list` / `use` / `current` / `tools` / `add` / `addtool` / `pin` / `restore` / `doctor`
- GitHub Actions 三平台自动构建与 Release 发布

### 兼容性说明

- 旧版 1.x 的配置文件 `~/.envswitch.json` 会在首次启动时自动迁移到 `~/.envswitch/config.json`，手动添加过的 Java/Python 路径不会丢。
